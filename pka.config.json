{
  "configVersion": 2,
  "_pkaGenerated": true,
  "mode": "flatten",
  "outputDir": "./project-knowledge",
  "maxFileSize": 1048576,
  "noGit": false,
  "noContext": false,
  "noConcat": false,
  "noFlatten": false,
  "install": false,
  "noGitignore": false,
  "xml": false,
  "agentsMd": null,
  "copilot": null,
  "cursorRules": null,
  "hierarchical": false,
  "scaffoldCommands": false,
  "compact": false,
  "compactTokens": 0,
  "compactOmit": false,
  "compactKeep": [],
  "compactPreview": false,
  "diff": false,
  "force": false,
  "watch": false,
  "since": null,
  "includeExt": [],
  "excludeDir": [],
  "instructions": "",
  "description": "**pka** is a single-file Node.js CLI (`index.js`) with zero runtime dependencies. It scans a project directory and generates AI-agent context files: `CLAUDE.md` for Claude Code, `AGENTS.md` for OpenAI Codex/Devin, `.github/copilot-instructions.md` for GitHub Copilot, and `.cursorrules` for Cursor.\n\nFour modes are supported: `flatten` (flattened source files + `CLAUDE.md` written to an output dir), `claude-code` (`CLAUDE.md` at the project root only), `multi-tool` (all agent files), and `full` (flatten + all agent files). All generated files carry a `<!-- pka-generated -->` marker so the tool can safely overwrite its own artifacts without touching user-authored files.\n\nAll logic lives in a single `ProjectAnalyzer` class. It collects metadata (`package.json`, git info, `.env.example`, `.claude/` settings), scans the file tree, builds an import graph, detects the tech stack from dependencies, and renders everything into Markdown. Config is read from `pka.config.json`, which is versioned via `configVersion` — the `init` subcommand generates or migrates it non-destructively, preserving existing settings and appending only the fields introduced in the new version."
}